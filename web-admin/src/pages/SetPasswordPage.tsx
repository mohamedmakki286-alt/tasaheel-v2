import {useState} from 'react';import {useNavigate,useSearchParams} from 'react-router-dom';import toast from 'react-hot-toast';import client from '../api/client';
export default function SetPasswordPage(){const[p]=useSearchParams(),nav=useNavigate(),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();if(password.length<8)return toast.error('كلمة المرور يجب ألا تقل عن 8 أحرف');if(password!==confirm)return toast.error('كلمتا المرور غير متطابقتين');const token=p.get('token');if(!token)return toast.error('رابط الدعوة غير صالح');setLoading(true);try{await client.post('/auth/password/reset',{token,newPassword:password});toast.success('تم إعداد الحساب، يمكنك تسجيل الدخول الآن');nav('/login',{replace:true})}catch(e:any){toast.error(e?.response?.data?.message||'الرابط منتهي أو غير صالح')}finally{setLoading(false)}}return <main dir="rtl" className="min-h-screen grid place-items-center bg-gray-50 dark:bg-surface-950 p-4"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white dark:bg-surface-900 border dark:border-surface-700 shadow-xl p-7 space-y-5"><img src="/tasaheel-app-icon.png" className="w-16 h-16 rounded-2xl mx-auto"/><div className="text-center"><h1 className="text-2xl font-black dark:text-white">إعداد حساب المشرف</h1><p className="text-sm text-gray-500 mt-2">أنشئ كلمة مرور آمنة للدخول إلى خدمة العملاء</p></div><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="كلمة المرور الجديدة" className="w-full rounded-xl border p-3 dark:bg-surface-800 dark:text-white"/><input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="تأكيد كلمة المرور" className="w-full rounded-xl border p-3 dark:bg-surface-800 dark:text-white"/><button disabled={loading} className="w-full rounded-xl bg-red-600 text-white font-bold p-3">{loading?'جاري الحفظ…':'إعداد الحساب'}</button></form></main>}
