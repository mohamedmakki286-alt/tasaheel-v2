version: "3.8"

services:
  tasaheel:
    build: .
    container_name: tasaheel-backend
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - uploads:/tmp/tasaheel/uploads

volumes:
  uploads:
